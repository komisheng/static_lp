// Custom JavaScript for landing page
;(($) => {
  // Declare the jQuery variable before using it
  var jQuery = window.jQuery

  // Add any custom interactions here
  $(document).ready(() => {
    console.log("Landing page loaded")
  })
})(window.jQuery)
